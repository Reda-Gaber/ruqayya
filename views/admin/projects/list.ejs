<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>جميع المشاريع - لوحة التحكم</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Cairo', sans-serif;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
    }
    .page-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 2rem 0;
      margin-bottom: 2rem;
      border-radius: 0 0 20px 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }
    .project-card {
      transition: all 0.4s ease;
      border: none;
      border-radius: 20px;
      overflow: hidden;
      background: white;
      box-shadow: 0 5px 15px rgba(0,0,0,0.08);
    }
    .project-card:hover {
      transform: translateY(-12px);
      box-shadow: 0 20px 40px rgba(102, 126, 234, 0.25);
    }
    .project-img {
      height: 220px;
      object-fit: cover;
      transition: transform 0.5s ease;
    }
    .project-card:hover .project-img {
      transform: scale(1.08);
    }
    .card-title {
      color: #2c3e50;
      font-weight: 700;
      margin-bottom: 0.75rem;
    }
    .card-text {
      color: #7f8c8d;
      line-height: 1.7;
      font-size: 0.95rem;
    }
    .btn-edit {
      background: linear-gradient(45deg, #f39c12, #e67e22);
      border: none;
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 12px;
      font-weight: 600;
    }
    .btn-delete {
      background: linear-gradient(45deg, #e74c3c, #c0392b);
      border: none;
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 12px;
      font-weight: 600;
    }
    .btn-add {
      background: linear-gradient(45deg, #27ae60, #2ecc71);
      border: none;
      padding: 0.75rem 2rem;
      border-radius: 50px;
      font-size: 1.1rem;
      box-shadow: 0 8px 25px rgba(46, 204, 113, 0.4);
    }
    .date-badge {
      background: rgba(102, 126, 234, 0.15);
      color: #667eea;
      padding: 0.4rem 0.9rem;
      border-radius: 50px;
      font-size: 0.85rem;
      font-weight: 600;
    }
    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
    }
    .empty-state i {
      font-size: 5rem;
      color: #bdc3c7;
      margin-bottom: 1.5rem;
    }
    @media (max-width: 768px) {
      .page-header h1 {
        font-size: 1.8rem;
      }
      .btn-add {
        width: 100%;
        margin-top: 1rem;
      }
    }
  </style>
</head>
<body>

  <!-- Header فخم -->
  <div class="page-header text-center">
    <div class="container">
      <h1><i class="fas fa-project-diagram me-3"></i>لوحة تحكم المشاريع</h1>
      <p class="mb-0 opacity-90">إدارة كاملة لجميع مشاريع مؤسسة رقية اليامي للمقاولات</p>
    </div>
  </div>

  <div class="container pb-5">

    <!-- أزرار الإضافة والتنبيهات -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center mb-4 gap-3">
      <h2 class="text-primary mb-0"><i class="fas fa-th-large"></i> جميع المشاريع</h2>
      <a href="/admin/projects/new" class="btn btn-success btn-add">
        <i class="fas fa-plus-circle"></i> إضافة مشروع جديد
      </a>
    </div>

    <!-- رسائل النجاح والخطأ -->
    <% if (success) { %>
      <div class="alert alert-success alert-dismissible fade show shadow-sm">
        <i class="fas fa-check-circle"></i> <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (error) { %>
      <div class="alert alert-danger alert-dismissible fade show shadow-sm">
        <i class="fas fa-exclamation-triangle"></i> <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <!-- لو مفيش مشاريع -->
    <% if (projects.length === 0) { %>
      <div class="empty-state">
        <i class="fas fa-folder-open"></i>
        <h3 class="text-muted mt-4">لا توجد مشاريع بعد</h3>
        <p class="text-muted">ابدأ بإضافة أول مشروع لمؤسستك الآن</p>
        <a href="/admin/projects/new" class="btn btn-primary btn-lg px-5 mt-3">
          <i class="fas fa-plus"></i> إضافة المشروع الأول
        </a>
      </div>
    <% } else { %>

      <!-- شبكة المشاريع -->
      <div class="row g-4">
        <% projects.forEach(project => { %>
          <div class="col-sm-6 col-lg-4 col-xxl-3">
            <div class="project-card h-100">
              <img src="<%= project.mainImage %>" class="project-img w-100" alt="<%= project.title %>" loading="lazy">
              <div class="card-body d-flex flex-column p-4">
                <h5 class="card-title"><%= project.title %></h5>
                <p class="card-text flex-grow-1">
                  <%= project.description.length > 90 ? project.description.substring(0, 90) + '...' : project.description %>
                </p>
                
                <div class="d-flex gap-2 mt-3">
                  <a href="/admin/projects/edit/<%= project._id %>" class="btn btn-edit btn-sm flex-fill">
                    <i class="fas fa-edit"></i> تعديل
                  </a>
                  <form action="/admin/projects/delete/<%= project._id %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-delete btn-sm flex-fill" 
                            onclick="return confirm('هل أنت متأكد من حذف هذا المشروع نهائيًا؟')">
                      <i class="fas fa-trash-alt"></i> حذف
                    </button>
                  </form>
                </div>

                <div class="mt-3 pt-3 border-top">
                  <span class="date-badge">
                    <i class="fas fa-calendar-alt me-1"></i>
                    <%= new Date(project.createdAt).toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                  </span>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>